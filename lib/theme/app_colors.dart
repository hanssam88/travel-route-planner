import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const sage = Color(0xFF5C8A6E);
  static const sageDark = Color(0xFF3F6B52);
  static const sageContainer = Color(0xFFD6E5DA);
  static const sageOnContainer = Color(0xFF1A3A24);

  static const terracotta = Color(0xFFC8654A);
  static const terracottaDark = Color(0xFFA84A33);
  static const terracottaContainer = Color(0xFFF4D9D0);

  // Neutral surfaces
  static const cream = Color(0xFFFAF6EF);
  static const creamHigh = Color(0xFFF2EBDD);
  static const creamHighest = Color(0xFFE9DFCB);

  static const ink = Color(0xFF1F2A24);
  static const inkSoft = Color(0xFF5A6660);
  static const inkMute = Color(0xFF8A958F);
  static const outline = Color(0xFFC9C0AE);
  static const divider = Color(0xFFE1D6BF);

  // Accents
  static const bookmark = Color(0xFFE8B84A);

  // Congestion (혼잡도 신호)
  static const congestionLow = Color(0xFF3FA86B);
  static const congestionMid = Color(0xFFE8B84A);
  static const congestionHigh = Color(0xFFD55D5D);

  // Source brand colors (for import source pills)
  static const srcInstagram = Color(0xFFE1306C);
  static const srcTiktok = Color(0xFF010101);
  static const srcNaver = Color(0xFF03C75A);
  static const srcKakao = Color(0xFFFEE500);
  static const srcGallery = Color(0xFF7E6BD9);
  static const srcManual = Color(0xFF5A6660);

  // Dark mode
  static const inkDark = Color(0xFF1B221E);
  static const inkDarkHigh = Color(0xFF242C27);
  static const sageLight = Color(0xFF9DC8AC);
}
